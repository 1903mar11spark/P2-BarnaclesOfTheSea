export class LoginCreds {
    id: number;
    uName: string;
    pWord: number;

    constructor(id: number, uName: string, pWord: number) {
        this.id = id;
        this.uName = uName;
        this.pWord = pWord;
    }
}